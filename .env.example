# zzz environment variables — copy to .env and edit

# Npcap SDK path (Windows)
# NPCAP_SDK=C:/path/to/npcap-sdk

# Zig compiler path (Windows override, otherwise uses PATH zig)
# ZIG=C:/path/to/zig.exe

# Inno Setup compiler
# ISCC=C:/path/to/ISCC.exe
